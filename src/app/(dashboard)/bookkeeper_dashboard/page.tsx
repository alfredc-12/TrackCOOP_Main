export default function BookkeeperDashboardPage({ searchParams }: { searchParams: { tab?: string } }) {
  // Completely empty for now, as requested.
  // We can read searchParams.tab here later to render different components.
  return <div />;
}
