export function RentalChartCard({ title, description, data, format = (value) => String(value) }: { title: string; description: string; data: Array<{ label: string; value: number }>; format?: (value: number) => string }) {
  const max = Math.max(...data.map((item) => item.value), 1);
  return <article className="rounded-2xl border border-[#dce7d6] bg-white p-5"><h2 className="text-lg font-extrabold text-[#123d2a]">{title}</h2><p className="mt-1 text-xs leading-5 text-[#6d7b72]">{description}</p><div className="mt-6 flex h-44 items-end gap-3" role="img" aria-label={`${title}: ${data.map((item) => `${item.label} ${format(item.value)}`).join(", ")}`}>{data.map((item) => <div key={item.label} className="flex h-full min-w-0 flex-1 flex-col justify-end gap-2 text-center"><span className="text-[10px] font-bold text-[#53675a]">{format(item.value)}</span><div className="mx-auto w-full max-w-12 rounded-t-lg bg-[#4b9163]" style={{ height: `${Math.max(8, item.value / max * 100)}%` }} /><span className="truncate text-[10px] font-semibold text-[#75837a]">{item.label}</span></div>)}</div></article>;
}
